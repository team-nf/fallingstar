services:
  coral:
    build:
      context: .
      dockerfile: Dockerfile
    privileged: true  # Required for USB device access
    volumes:
      - /dev/bus/usb:/dev/bus/usb  # Mount USB devices
      - .:/app  # Mount current directory as working directory
    # Keep the container running for development
    stdin_open: true
    tty: true 