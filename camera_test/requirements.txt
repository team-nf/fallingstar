# Requirements for Coral TPU
# Most dependencies are already installed in the base image
# We only need minimal additional packages
matplotlib>=3.0.0,<3.3.0
# Using system-provided opencv-python package
numpy>=1.16.0,<1.19.0
Pillow>=6.0.0,<8.0.0 